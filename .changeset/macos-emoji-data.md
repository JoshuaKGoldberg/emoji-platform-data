---
"@emoji-platform-data/generator": minor
"@emoji-platform-data/macos": minor
"emoji-platform-data": minor
---

Added macOS emoji data, as a new `@emoji-platform-data/macos` package and a `macos` property on `EmojiPlatformData`.

Each `MacOSItem` holds the keywords macOS's emoji picker searches, along with the emoji's Apple name, VoiceOver and speech names, picker category, and picker order.
This also adds 23 emoji that none of the other sources know yet, such as 🫩, 🫆, 🪉, and 🫜.
